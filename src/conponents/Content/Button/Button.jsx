
const Button = (props) => {
    return (
    <>
        <button type="button"><img className={props.style.button} src={props.Basket} alt={props.Basket}></img></button>
    </>
    )
}
export default Button;